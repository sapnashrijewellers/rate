{
  "type": "module",
  "dependencies": {
    "gh-pages": "^6.3.0",
    "web-push": "^3.6.7",
    "socket.io-client": "^4.8.1"
  },
  "scripts": {
    "generate:rates": "node rates.js",
    "publish:rates": "npm run generate:rates && rm -rf node_modules/.cache/gh-pages && mkdir -p tmp-rates && cp rates.json tmp-rates/rates.json && gh-pages -d tmp-rates -b gh-data -m \"Update rates.json\" --repo \"https://${GITHUB_TOKEN}@github.com/sapnashrijewellers/rate.git\" && rm -rf tmp-rates",
    "rates:notification": "node sendNotification.js"
  }
}